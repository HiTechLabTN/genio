# Requirements for VODER

# GUI Framework
pyqt5>=5.15.0

# Core Audio Processing, or just core
numpy>=1.21.0
scipy>=1.10.0
soundfile>=0.12.0
sounddevice>=0.5.0
pydub>=0.25.0
diffusers
psutil

# PyTorch (conservative version for compatibility)
torch==2.8.0
torchaudio==2.8.0
torch-audiomentations>=0.12.0
torchvision==0.23.0
torchcodec
vector_quantize_pytorch

# ML/AI Models
openai-whisper>=20231117
transformers==4.57.3
accelerate>=0.20.0
huggingface-hub==0.34.0
einops>=0.6.0

# BS-RoFormer (SVS - Song Voice Separate)
rotary_embedding_torch==0.3.5
beartype==0.14.1
ml_collections

# Fish-Speech (S2 Pro TTS)
loralib>=0.1.2
resampy>=0.4.3
natsort>=8.4.0
tiktoken>=0.8.0
zstandard>=0.22.0

# AudioSR (Super Resolution)
timm>=0.9.8,!=1.0.*
ftfy
regex
scikit-learn
unidecode
phonemizer
pandas
chardet
torchlibrosa==0.0.9

# Configuration
omegaconf>=2.3.0
PyYAML>=6.0
hydra-core==1.3.2
opentelemetry-proto==1.0.0
opentelemetry-exporter-otlp-proto-grpc==1.0.0

# TTS and more ofc
# source contains "qwen-tts==0.1.1"
sox
onnxruntime
ctc-forced-aligner>=1.0.0

# Voice Conversion & Music Generation
munch>=4.0.0
gradio>=4.0.0
funasr>=1.0.0
modelscope>=1.9.0
librosa>=0.10.0
loguru
descript-audiotools>=0.7.2

# Speech Enhancement (UniSE)
einx
x-transformers==2.3.1
safetensors
soxr
tqdm
packaging

# Speaker Diarization
pyannote.database>=5.0.0
pyannote.metrics>=3.0.0
pyannote.pipeline>=3.0.0
lightning==2.4
asteroid-filterbanks>=0.4.0

# YouTube/Audio Download
yt-dlp>=2026.3.17
gallery-dl>=1.27.0

# VADAR chat (Gemma 4 GGUF via Ollama — used by eva ttt)
ollama>=0.4.0

# Image/Video processing (used by Klarify DLC and Eva downscale helper)
opencv-python>=4.9.0.80

# OCR for image text extraction
easyocr>=1.7.0

# Language Detection (auto source language for TranslateGemma)
langdetect>=1.0.9
#IMPORTANT:
#AFTER DOWNLOADING REQUIREMENTS
#pip install --upgrade protobuf==5.29.6

# Note: For optimal performance on different platforms:
# - Linux: Install PyTorch with CUDA support separately if needed
# - macOS: PyTorch with MPS support available for Apple Silicon
# - Windows: PyTorch with CUDA support available
librosa>=0.10.0
Pillow>=10.0.0
